providers = ["node"]

[variables]
NODE_VERSION = "18"
NODE_ENV = "production"

[phases.install]
cmds = ["cd backend && npm ci"]

[phases.build]
cmds = ["cd backend && npm run build"]

[phases.start]
cmd = "cd backend && npm start"