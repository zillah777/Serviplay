{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install && npm run build && node check-db.js && npm run migrate"
  },
  "deploy": {
    "startCommand": "cd backend && node dist/index.js"
  }
}